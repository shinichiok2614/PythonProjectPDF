pip install pdf2image pillow

📌 Windows cần cài thêm Poppler
Tải Poppler (bản Windows):
https://github.com/oschwartz10612/poppler-windows/releases/

Giải nén → lấy đường dẫn bin/ → add vào PATH.

pip install pdf2image pillow PyPDF2
pip install PyPDF2
pip install PyMuPDF